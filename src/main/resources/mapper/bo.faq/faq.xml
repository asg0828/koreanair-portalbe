<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.cdp.portal.app.bo.faq.mapper.FaqMapper">

    <select id="selectFaqList" resultType="FaqResDto">
        SELECT
        *
        FROM (
        SELECT
        faq.faq_id,
        faq.cl_code,
        faq.qstn,
        faq.answ,
        faq.ord_seq,
        faq.use_yn,
        faq.del_yn,
        faq.rgst_id,
        faq.rgst_dt,
        faq.modi_id,
        faq.modi_dt,
        faq.view_cnt,
        tu.user_nm 		AS RGST_NM
        FROM
        t_bbs_faq faq
        LEFT OUTER JOIN T_USER tu ON faq.RGST_ID = tu.user_id
        ) A
<!--        <include refid="where" />-->
<!--        <include refid="order" />-->
<!--        <include refid="paging.pagingOffsetSQL" />-->
    </select>

</mapper>

