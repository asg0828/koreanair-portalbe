<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cdp.portal.app.facade.user.mapper.UserMgmtUserAuthMapper">

    <select id="selectAll" resultType="UserMgmtUserAuthResDto">
        SELECT ROW_NUMBER() OVER (ORDER BY RGST_DT ASC) AS ROWNUM
		     , AUTH_ID 
		     , AUTH_CL 
		     , AUTH_NM 
		     , AUTH_DSC 
		  FROM T_USER_SYS_AUTH USA
		 WHERE USE_YN = 'Y'
    </select>

</mapper>