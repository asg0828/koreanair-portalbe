<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cdp.portal.app.facade.user.mapper.UserMgmtMgrAuthMapper">

    <select id="selectAll" resultType="UserMgmtMgrAuthResDto">
        SELECT ROW_NUMBER() OVER (ORDER BY RGST_DT ASC) AS ROWNUM
		     , AUTH_ID 
		     , AUTH_CL 
		     , AUTH_NM 
		     , AUTH_DSC 
		  FROM T_MGR_SYS_AUTH MSA
		 WHERE USE_YN = 'Y'
    </select>

</mapper>