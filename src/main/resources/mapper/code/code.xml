<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.cdp.ap.app.mapper.CodeMapper">
	
	<select id="selectGroupIdAllList" resultType="CodeResDto">
		SELECT
			ROW_NUMBER() OVER (ORDER BY ORD_SEQ ASC) AS ROWNUM,
			GROUP_ID,
			CODE_ID,
			CODE_NM,
			CODE_DSC,
			ORD_SEQ,
			USE_YN,
			RGST_ID,
			RGST_DT,
			MODI_ID,
			MODI_DT
		FROM
			T_CODE
		WHERE
			GROUP_ID = #{groupId}
		ORDER BY ROWNUM
	</select>

</mapper>

